apiVersion: autoscaling.agones.dev/v1
kind: FleetAutoscaler
metadata:
  name: blocksumo
  namespace: emortalmc

spec:
  fleetName: blocksumo
  policy:
    type: Buffer
    buffer:
      bufferSize: 1
      minReplicas: 3
      maxReplicas: 5
